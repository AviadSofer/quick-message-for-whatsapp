import styled from 'styled-components';

const FlagIcon = styled.img`
width: 1rem;

border-radius: 4px;
box-shadow: 0px 4px 9px -4px rgba(135,135,135,0.82);

pointer-events: none;
`;

export default FlagIcon;
